apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plcc-edge.fullname" . }}-test-connection"
  labels:
    {{- include "plcc-edge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plcc-edge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
